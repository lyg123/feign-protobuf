server:
  port: 9092

#management.security.enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8011/eureka/
  instance:
    prefer-ip-address: true
spring:
  application:
    name: user
